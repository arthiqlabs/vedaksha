# Vedaksha arXiv preprint

Source for the software/methods paper submitted to arXiv (see
`docs/plans/2026-08-28-arxiv-vedaksha-preprint.md` for how this was produced and verified).

Build: `make pdf` (requires `tectonic`, installed via `brew install tectonic`). Output lands in
`build/main.pdf`.
